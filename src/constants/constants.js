export const CARD_HEIGHT = 150; // height of a single card(excluding marginBottom/paddingBottom)
export const OFFSET_HEIGHT = 64 + 10 + 8 + 50; // height offset from the top:header + groupMargin + groupPadding + titleHeight+cardMargin
export const CARD_MARGIN = 8 + 0; // height of a marginBottom + paddingBottom